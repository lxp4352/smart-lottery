<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.easyjava.lottery.infrastructure.strategy.dao.IStrategyDetailDao">

    <update id="deductStock" parameterType="cn.easyjava.lottery.infrastructure.strategy.po.StrategyDetail">
        UPDATE strategy_detail SET award_surplus_count = award_surplus_count - 1
        WHERE strategy_id = #{strategyId} AND award_id = #{awardId} AND award_surplus_count > 0
    </update>

</mapper>
